
# AI 운항 리포팅 실행 프롬프트 (2단계용)
AI_REPORT.md와 나란히 놓고 사용합니다.

전제: BOOTSTRAP_PROMPTS.md 1단계 완료(로그인 후 사이드바가 뜨는 상태). 이 문서는 전달 수단, AI_REPORT.md가 내용입니다.

규모: 원본 약 1,200줄 · 난이도 ★★ · 체크리스트 14항목 계산 로직이 많은 화면이라 2단계로 나누는 것을 권합니다.



## 0. 세션 시작 시 한 번
지금부터 docs/specs/AI_REPORT.md 를 보고 "AI 운항 리포팅" 화면을 만든다.
이 명세가 유일한 기준이며, 명세에 없는 것은 임의로 추가하지 않는다.

- 코드를 쓰기 전에 해당 장을 먼저 읽어라.
- 12장(엣지 케이스)을 먼저 훑어보고 시작해라.
- 6장의 계산 함수는 전부 순수 함수로 만들어라. 화면과 서버 양쪽에서 재사용해야 한다.
- 명세와 다르게 구현해야 할 이유가 있으면, 만들기 전에 먼저 나에게 말해라.



## 1. 화면과 계산 (Gemini 없이)
AI_REPORT.md 16장 · 911장 · 여기까지만 해도 화면이 완결됩니다.

docs/specs/AI_REPORT.md 를 읽고 AI 운항 리포팅 화면을 만들어줘.
src/app/(main)/ai-report/page.tsx 부터 시작한다.

이번 범위는 7장(Gemini 재분석)을 뺀 전부다:
- 3장 데이터 모델 + 샘플 (선박 4·항차 4·AIS 4·리포트 4)
- 4장 화면 구성 (아코디언 카드, 헤더 요약 3지표, 펼침 영역 9블록)
- 5장 공용 컴포넌트 5종 (StatCard / ProbabilityGauge / VoyageProgressLine /
  ReportWeatherStats / RiskBadge)
- 6장 계산 로직 전부
- 9장 PDF 내보내기

"재분석" 버튼은 UI만 만들고 클릭 시 아무 동작도 하지 않게 둔다.
baseline mock 데이터만으로 화면이 완전히 동작해야 한다.

6장에서 특히 주의할 것:
- 6.4장 "앵커 비율 방식"을 반드시 지켜라. 남은 거리를 속도로 직접 나누지 마라.
  mock 데이터는 거리·속도·일정이 물리적으로 안 맞아서, 그렇게 하면 시나리오마다 결과가 어긋난다.
- 권장 속도든 현재 속도든 marginToProbability() 하나만 쓴다.
  권장 속도라고 100%를 하드코딩하지 마라.
- RTA 미확정이면 STA를 쓰는 분기는 deadlineIso를 만드는 한 곳에만 둔다.

다 만들면 13장 재구현 체크리스트로 누락을 확인해줘.

완료 확인 — 리포트 카드 4장이 뜨고 첫 카드가 펼쳐져 있다. 게이지·진행률·스탯 카드에 숫자가 채워지고, voy001에만 "RTA 미확정" 호박색 배너가 보인다.

계산 함수를 물류 일정 관리 화면과 공유하세요. SCHEDULE.md 6.1장의 "준수 확률" 열이 6.1~6.4장의 함수를 그대로 재사용합니다. 화면마다 따로 구현하면 같은 항차가 다른 확률로 보입니다 — 원본에서 이걸로 전 화면을 재작업한 적이 있습니다 (KNOWN_PITFALLS.md 5.2).



## 2. (선택) Gemini 재분석
AI_REPORT.md 7장 · API 키가 없으면 건너뛰세요. 화면은 이미 완결되어 있습니다.

docs/specs/AI_REPORT.md 7장대로 Gemini 재분석을 붙여줘.

- 7.2장 요청 payload, 7.3장 서버 처리(프롬프트 전문 포함), 7.4장 응답 후처리,
  7.5장 클라이언트 반영 규칙을 그대로 구현한다.
- 키가 없으면 { ok: false, reason: 'no_api_key' }를 HTTP 200으로 반환하고,
  클라이언트는 baseline으로 폴백한 뒤 호박색 안내를 띄운다.
- 7.4장의 "속도 강제 보정"을 반드시 구현해라. 모델이 범위를 벗어난 속도를 제안해도
  서버가 최종적으로 보정해야 "권장 속도는 항상 마감을 지킨다"는 전제가 유지된다.
- 언어를 전환하면 override를 무효화하고 baseline으로 되돌린다.

완료 확인 — 키가 없어도 "재분석"을 눌렀을 때 화면이 깨지지 않고 안내만 뜬다.



## 3. 완료 검증
docs/specs/AI_REPORT.md 13장 재구현 체크리스트 14항목을 확인해줘.
그다음 12장 엣지 케이스 8개를 훑어보고 위반한 것이 있는지 점검해줘.



## 이 화면의 함정

| 항목 | 내용 |
| --- | --- |
| PDF 한글 | Track A: 반입한 public/fonts/NotoSansKR-Regular.ttf 를 그대로 fetch. Track B: PDF를 영문 전용으로(SIMULATION.md 7.1장 방식) |
| 계산 공유 | KNOWN_PITFALLS.md 5.2 |
| 딥링크 스크롤 | 펼침 상태를 flushSync로 먼저 커밋한 뒤 스크롤해야 위치가 맞는다 — KNOWN_PITFALLS.md 1.4 |
| 딥링크 키 | 대시보드가 보내는 정확한 키는 ksf:ai-report-vessel-id (AI_REPORT.md 8장) |
| 카드 헤더 버튼 중첩 | 4.2장 카드 헤더(펼침/접힘 토글) 안에 PDF 다운로드·재분석처럼 실제 동작하는 `<button>`을 넣으면 `<button>` 안에 `<button>`이 중첩되어 hydration 에러가 난다. 바깥 토글을 `<div role="button" tabIndex={0}>`로 구현할 것 — KNOWN_PITFALLS.md 1.8 |
| Open-Meteo 서버 재사용 | 7.3장(서버 재조회)이 6.7장의 클라이언트 훅 파일을 그대로 import하면 "client 훅을 import했다"는 빌드 에러가 난다. fetch 로직을 훅 없는 별도 파일로 분리해 양쪽이 그 파일을 import할 것 — KNOWN_PITFALLS.md 1.9 |

