
# 물류 일정 관리 실행 프롬프트 (2단계용)
SCHEDULE.md와 나란히 놓고 사용합니다.

전제: BOOTSTRAP_PROMPTS.md 1단계 완료(로그인 후 사이드바가 뜨는 상태). 이 문서는 전달 수단, SCHEDULE.md가 내용입니다.

규모: 원본 약 950줄 · 난이도 ★★ · 체크리스트 19항목



## 0. 세션 시작 시 한 번
지금부터 docs/specs/SCHEDULE.md 를 보고 "물류 일정 관리" 화면을 만든다.
이 명세가 유일한 기준이며, 명세에 없는 것은 임의로 추가하지 않는다.

- 코드를 쓰기 전에 해당 장을 먼저 읽어라.
- 11장(엣지 케이스)을 먼저 훑어보고 시작해라. 놓치기 쉬운 것만 모아둔 장이다.
- 명세와 다르게 구현해야 할 이유가 있으면, 만들기 전에 먼저 나에게 말해라.



## 1. 기본 — 스펙을 통째로
이 화면은 한 번에 시켜도 품질이 나옵니다. 아래 프롬프트 하나로 시작하세요.

docs/specs/SCHEDULE.md 를 처음부터 끝까지 읽고 물류 일정 관리 화면을 만들어줘.
src/app/(main)/schedule/page.tsx 부터 시작한다.

범위:
- 4장 화면 구성 전체 (헤더 / 뷰 탭 / 검색·상태필터 / 목록 테이블 / 캘린더)
- 5장 항차 등록·조회 모달
- 6장 계산 로직 (6.1 준수 확률은 아래 참고)
- 7장 상태별 편집 정책

지금은 6.1장 "준수 확률" 열을 생략하고 "—"만 표시해라.
나머지가 다 된 뒤에 별도로 요청하겠다.

다 만들면 12장 재구현 체크리스트로 누락을 확인해줘.

완료 확인 — 목록에 항차 8건이 뜨고, 상태 필터·검색이 동작하며, 캘린더 탭에 마커가 보인다.



## 2. (선택) 준수 확률 열
SCHEDULE.md 6.1장 · 시간이 부족하면 생략해도 화면은 완전합니다.

docs/specs/SCHEDULE.md 6.1장대로 목록의 "준수 확률" 열을 구현해줘.

계산 체인: 항해 진행률(haversine) → 앵커 비율로 ETA 환산 → 마진 → 확률
- 리포트나 선박을 못 찾으면 null을 반환해 "—"를 표시한다(신규 항차는 항상 그렇다).
- rtaConfirmed에 따라 RTA/STA 중 하나를 deadlineIso로 넘긴다. 이 분기는 한 곳에만 둔다.
- STA 기준이면 색이 항상 파랑, RTA 기준이면 신뢰도별 초록/노랑/빨강이다.

중요: 이 계산은 AI 운항 리포팅 화면과 완전히 동일한 함수를 써야 한다.
그 화면 담당자와 상의해 순수 함수를 한 곳(features/ai-report/ 또는 shared/)에 두고 공유해라.
화면마다 따로 구현하면 같은 항차가 다른 확률로 보인다.



## 3. 완료 검증
docs/specs/SCHEDULE.md 12장 재구현 체크리스트 19항목을 하나씩 확인해줘.
그다음 11장 엣지 케이스 14개를 훑어보고 위반한 것이 있는지 점검해줘.
직접 확인이 필요한 항목은 무엇을 눌러봐야 하는지 알려줘.



## 이 화면의 함정
구현 중 아래 증상이 나오면 KNOWN_PITFALLS.md에서 해당 항목을 보세요. 모두 원본에서 실제로 발생한 것이며, 명세에도 주의로 표시되어 있습니다.


| 증상 | 원인 | 항목 |
| --- | --- | --- |
| 목록과 상세 팝업의 시·분이 다름 | ISO를 slice(0,16)으로 자름 | 3.1 |
| 날짜 고르기 전 시·분 선택이 사라짐 | 날짜 미선택 시 빈 문자열 반환 | 3.2 |
| 오전/오후 드롭다운 글자가 안 보임 | 네이티브 화살표가 텍스트를 밀어냄 | 3.3 |
| RTA 경고 문구가 ETA 박스를 침범 | 음수 마진으로 붙임 | 4.4 |


Track B(항로 JSON 없음)라면 SCHEDULE.md 6.3장의 haversine × 1.25 폴백을 씁니다. ETA 자동 계산과 거리 표시가 정상 동작하며, 지도 항로만 직선으로 보입니다.
