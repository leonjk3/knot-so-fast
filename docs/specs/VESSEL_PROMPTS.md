
# 선박 관리 실행 프롬프트 (2단계용)
VESSEL.md와 나란히 놓고 사용합니다.

전제: BOOTSTRAP_PROMPTS.md 1단계 완료(로그인 후 사이드바가 뜨는 상태). 이 문서는 전달 수단, VESSEL.md가 내용입니다.

규모: 원본 약 790줄 · 난이도 ★ · 체크리스트 19항목



## 0. 세션 시작 시 한 번
지금부터 docs/specs/VESSEL.md 를 보고 "선박 관리" 화면을 만든다.
이 명세가 유일한 기준이며, 명세에 없는 것은 임의로 추가하지 않는다.

- 코드를 쓰기 전에 해당 장을 먼저 읽어라.
- 11장(엣지 케이스)을 먼저 훑어보고 시작해라.
- 명세와 다르게 구현해야 할 이유가 있으면, 만들기 전에 먼저 나에게 말해라.



## 1. 기본 — 스펙을 통째로
이 화면은 한 번에 시켜도 품질이 나옵니다. 아래 프롬프트 하나로 시작하세요.

docs/specs/VESSEL.md 를 처음부터 끝까지 읽고 선박 관리 화면을 만들어줘.
src/app/(main)/vessel/page.tsx 부터 시작한다.

범위:
- 4장 화면 구성 (헤더 / 검색 / 카드 그리드 / 우측 상세 패널)
- 5장 연료 소모 커브 차트 (ECharts)
- 6장 선박 등록·수정 모달
- 7장 필드 잠금 정책
- 8장 계산 로직

주의:
- 차트는 dynamic(() => import('echarts-for-react'), { ssr: false }) 로 로드하고
  notMerge를 켜라. 안 그러면 SSR에서 깨지거나 선박을 바꿔도 이전 옵션이 남는다.
- 노후 계수 경고는 "> 1.07" 초과 비교다. >= 로 하면 샘플 v002(정확히 1.07)까지
  주황색이 되어 원본과 달라진다.
- 이 화면의 모달은 다국어를 쓰지 않는다. 라벨·오류 메시지를 한국어로 하드코딩한다
  (원본 그대로. 6장 첫머리 주의 참고).

다 만들면 12장 재구현 체크리스트로 누락을 확인해줘.

완료 확인 — 카드 5장이 뜨고, 카드를 누르면 우측 패널과 연료 커브 차트가 열린다. v003(1.09)만 노후 계수가 주황색이고 v002(1.07)는 아니다.



## 2. (선택) 차트를 나중으로 미루기
차트 라이브러리 도입이 부담되면 표만 먼저 만들고 나중에 붙여도 됩니다.

지금은 연료 소모 커브 차트를 생략하고, 상세 패널에 속도·소모량 표만 만들어줘.
차트는 나중에 docs/specs/VESSEL.md 5장을 보고 붙이겠다.

정보 손실은 없습니다. 다만 이 화면에서 시각적 밀도가 가장 높은 요소라 데모를 위해서는 가급적 구현을 권합니다.



## 3. 완료 검증
docs/specs/VESSEL.md 12장 재구현 체크리스트 19항목을 하나씩 확인해줘.
그다음 11장 엣지 케이스 16개를 훑어보고 위반한 것이 있는지 점검해줘.
특히 7장 필드 잠금 정책이 정확히 적용됐는지(항상 잠김 10개 / 활성 항차 시 상태 잠김 /
항상 수정 가능 4개) 표로 대조해줘.



## 이 화면의 함정

| 증상 | 원인 | 항목 |
| --- | --- | --- |
| window is not defined | ECharts를 정적 import | KNOWN_PITFALLS.md 1.7 계열 · VESSEL.md 11장 11번 |
| 선박을 바꿔도 차트가 안 바뀜 | notMerge 누락 | VESSEL.md 11장 12번 |
| markLine 라벨이 이상함 | 평균값이 아니라 노후 계수를 표시하는 것이 정상 | VESSEL.md 5장 |


혼동하기 쉬운 지점 — VESSEL.md 8.1장의 해군 배수량 법칙 검산표(10→41)와 3.2장 샘플 커브(10→52)가 일치하지 않습니다. 샘플은 손으로 쓴 값이고 자동 생성 공식은 신규 등록 시에만 쓰이기 때문입니다. 버그가 아니니 맞추려 하지 마세요.
